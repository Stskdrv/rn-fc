import { extendTheme } from "native-base";

const theme = extendTheme({
    colors: {
        primary: {
            50: '#FFFAF0',
            100: '#DCDCDC',
            150: '#313135',
            200: '#202021',
            250: '#323232',
        }
    }
});

export default theme;